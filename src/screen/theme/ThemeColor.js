import {COLORS} from '../../constants';

export const light = {
  color: COLORS.dark,
  background: COLORS.light,
};
export const dark = {
  color: COLORS.light,
  background: COLORS.dark,
};
